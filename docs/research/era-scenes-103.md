# 재부팅 후 시대별 사건 자료 통합

실제 Claude Opus 5 / Max 조사 9개가 완료됐다. 일곱 사건 수집 작업의 새 Claim은 915개이고, 기존 장면을 같은 ID로 교체하며 합친 결과는 145개다. 북부 산줄기와 지역 좌표 수집은 별도 지리 자료다. 개발·통합은 Codex가 수행했다.

7개 수집 보고서: ancient-scenes-103.json, goryeo-scenes-103.json, joseon-early-scenes-103.json, joseon-late-scenes-103.json, modern-scenes-103.json, contemporary-scenes-103.json, harbors-scenes-103.json. 각 작업의 실제 모델·세션·다운로드 목록·출처와 짧은 발췌는 data/research/scenes-103 아래에 보존했다.

재부팅으로 끊긴 세션의 재개 실패, 원자료 보존과 새 실행은 reboot-recovery-103.md에 기록했다. 추가로 조선 전기 작업의 감독 프로세스가 Windows 파일 공유 잠금에서 종료된 것을 확인했다. 해당 작업의 확인된 자식 프로세스만 중단하고 같은 Claude 세션을 재개해 저장된 결과를 마무리했다. 원래 실행의 정상 종료로 기록하지 않았다.

## 연결 검사

- 모든 장면의 Claim 참조를 저장소 전체 11,651개 Claim과 대조: 미존재 참조 0개.
- 기본 5개 출판물 묶음에서 사건 날짜·행동·장소의 필수 근거가 필터로 누락되는 장면: 0개.
- 저장된 지명·유적·좌표 등록부를 넣은 계획 함수에서 137개는 좌표가 연결된다. 이는 브라우저 렌더링 통과 수가 아니다.
- 8개는 지점 미확인 또는 한반도 표시 범위 밖이다. 진포·평화시장 및 누리호의 직접 장소 근거는 별도의 작은 Opus 보강 작업을 시작했다. 다른 연도의 활동이나 생몰년을 현재 위치로 바꾸지 않는다.
- 수도 활동으로 명시한 조선 전기 5개 장면은 도읍의 지역 기준 추정 배치다. 관련 인물의 현장 출석을 뜻하지 않는다.
- 명량의 적군 전선은 이미 수집된 일본 수군 전선 근거에 연결했다. 기벌포의 당군과 신라군은 수집된 각 참가자 역할에 맞춰 서로 다른 편으로 연결했다.
- 지명 좌표를 설명하는 보조 Claim을 사건 행동의 필수 선택 조건과 분리했다. 서로 다른 URL을 가리키는 동일 출처 ID는 문서를 합치지 않고 별도 ID로 저장했다.

Python 검증기 OK, unittest 116개 PASS, 연대·조형·자료 로드·좌표 JS 검사 PASS. 새 자료의 공개 화면 검사는 배포 후 별도 기록한다. CI NOT_RUN: GitHub Actions workflow 없음.

모든 연도를 채웠거나 한국사를 전부 수록한 상태라는 뜻은 아니다. 출처가 밝힌 사건 시기만 표시하며, 수집 작업이 남긴 결손·다른 해석은 원자료와 장면 자료에 유지한다.
