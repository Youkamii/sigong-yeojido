# 시공여지도 문구·데이터 기준서 v2 (2026-09-16 사용자 결정 반영)

사용자 결정(질문 1~12 → b a b b a b b b a b a a). 이 문서가 copy-style-193.md 를 대체한다. 규칙마다 정의 1줄 + 예 1개 + 검사 1줄.

## 0. 운용
- 리더 Fable(max): 기획·지시서·판정·실측·커밋. **개발(코드·데이터 수정) = Opus 5 high 하위 에이전트**(워크트리별 1개, 동시 2개). **Codex = AI 이미지 생성만.** 조사·검증 워크플로 = Opus 5 high.

## 1. 문체 표 (대상별로 고정)
| 대상 | 문체 | 예 | 검사 |
|---|---|---|---|
| 화면 안내문·빈 상태·오류·툴팁·aria | **-ㅂ니다체**(격식). 어색한 곳만 고침 (Q3=b) | "연결된 출처가 없습니다." | 정규식 `해요\|어요\|예요\|세요` 0건 (UI 코드) |
| 버튼·탭·모드 전환 | 명사/짧은 동사형. 모드 탭은 **짧은 이름** (Q4=b) | 지도 · 3D · 연결 · 질문 · 비교 | 2~5어절 |
| 장면 요약(summary) | **교과서식 -다체, 과거는 '하였다/되었다'로 통일** (Q2=a), 짧은 문장·번역투 제거로 **전체 2차 손질** (Q1=b) | "…통제사에 임명되었다." | 한 문장 40자 안팎, `했다\|됐다` 0건, S1 잔존 0 |
| 설명문(describedAs) | 위와 같음(-다체/명사형, 하였다/되었다) | | 동일 |
| 좌표 근거 메모(coordinateNote) | -다체. **개발 용어 159건 우리말로 고쳐 카드에 다시 표시** (Q6=b) | anchor→기준점, precision area→일대 기준, 파일명·JSON-LD→"내려받은 페이지의 좌표" | `lon\|lat\|precision\|anchor\|json\|html\|hgis` 0건 |
| 역할(role) | ≤12자 직함 명사구, 홑명사 금지 | "조선 수군 지휘관" | 길이·어절 포함 검사 |
| 제목(title) | main(범주 접두어 제거·장면 연도와 같은 끝 괄호 연도 제거) + 장소 부제 삭제 + 이야기형 부제 `main, 부제`, 합쳐 30자 이하 | "전태일, 근로기준법을 지키라" | startswith(main), ≤30자, 대시 0 |
| 개체 이름 | **원본 데이터도 정리** (Q7=b): 연도·날짜 괄호, 11자 이상 설명 괄호, ' · ' 꼬리, '집단 행위자' 제거. 짧은 구분 괄호 유지. 검색·동일성 병합 재검증 필수 | "아관파천 (1896년 2월 11일)"→"아관파천" | 충돌(같은 이름 다른 개체) 목록 보고, 검색 회귀 테스트 |

## 2. 용어
- **'사료'를 되살린다** (Q9=a): 화면 첫 등장에 "사료(역사 기록)" 풀이, 이후 '사료'. #194 에서 '자료'로 바꾼 57곳 되돌림. 단 자료(데이터 파일·출처 설정 메뉴)를 뜻하는 곳은 '자료' 유지.
- 온톨로지·개발 용어 금지(개체·행위자·주장·정본·렌즈·패킷·근거 로드) → 항목/사람/장소, 기록, 기준 사료.
- 나라가 아닌 집단은 유형 이름 '집단'. '세력' 안 씀.
- 모르는 값 '미확인'(값 자리) / 비움(breadcrumb) / AI 결과 사람 검토 전 '사람 확인 전'.
- 연도 범위 '1545년~1598년'(물결표, 공백 없음). 대시(—·–) 금지.
- 영어 예외: AI, 3D, SIGONG YEOJIDO(브랜드), CRS84, km².
- 시작 화면 소개문은 현재 문장 유지 (Q10=b), 단 문체는 §1 규칙(-ㅂ니다)로.

## 3. 카드(좌측 패널)
- 초상은 **카드 폭 전체**로 크게 (Q8=b), 누르면 원본. 배지 'AI 상상도'만. **고지 문장 완전 삭제** (Q5=a): 배지 title·ai-images.js notice 기본값·finalize NOTICE 상수·index.json notice 표시 경로 모두 제거(alt 는 그림 내용 설명으로만).
- 좌표 근거 메모를 '지도 위치' 아래 다시 표시 (Q6=b).
- 그 밖(요약 먼저·탭·병합·출처명 「」)은 #193/#197 그대로 (Q12=a).

## 4. 성능
- 연도 이동 잔여 비용: **장면 조립 워커 분리 지금 진행** (Q11=a) — #196 후속.

## 5. 결정 기록
2026-09-16 사용자: Q1 b · Q2 a · Q3 b · Q4 b · Q5 a · Q6 b · Q7 b · Q8 b · Q9 a · Q10 b · Q11 a · Q12 a. 개발 Opus 5 high, Codex 이미지만.
미결(다음 질문): 1987 근접 화면 검은 건물 재질(조형도 교체 vs 철 재질 전역), 가로 폰 640×360 패널 높이 규칙.
