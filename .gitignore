# 벌크 원문 아카이브 — 재현 가능하므로 저장소에 넣지 않는다
# (scripts/fetch_datago_bulk.py 로 다시 받는다. 실록은 853MB다)
data/bulk/*.zip
data/bulk/*/

# 실록 30개 판본의 생성 JSONL(전체 약 802 MiB). 카드·수치·재현 명령은 커밋한다 (#15).
data/sources/sillok-*/*.jsonl
data/sources/seungjeongwon-ilgi/*.jsonl
data/sources/bibyeonsa-deungnok/*.jsonl

# 대동여지도 원본 스캔 99MB — scripts/fetch_daedong.py 로 다시 받는다.
# 웹용으로 줄인 daedong-*.jpg 만 저장소에 둔다.
services/host/assets/Daedongyeojido-full.jpg
services/host/assets/Daedongyeojido-small.png

# 자산 준비용 가상환경 (Pillow) — 실행에는 필요 없다
.venv-build/

__pycache__/
*.pyc

# TTL 빌드 산출물 — services/build_ttl.py 로 다시 만든다 (#5)
data/build/
.fuseki/

# NDL 경국대전 원해상도 스캔. manifest·면별 해시와 재현 명령은 추적한다 (#87).
data/scans/ndl-gyeongguk-1934/
